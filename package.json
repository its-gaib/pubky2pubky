{
  "name": "hole-punchky-specifications",
  "version": "0.1.0",
  "private": true,
  "description": "Reproducible validators for Hole Punchky API specifications",
  "engines": {
    "node": ">=20.19"
  },
  "scripts": {
    "lint:asyncapi": "node scripts/validate-asyncapi.mjs",
    "lint:openapi": "redocly lint docs/openapi.yaml",
    "lint:schemas": "npm run lint:openapi && npm run lint:asyncapi"
  },
  "devDependencies": {
    "@asyncapi/parser": "3.6.3",
    "@redocly/cli": "2.51.0"
  }
}
